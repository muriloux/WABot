import { all } from "./allCommand";
import { dice } from "./diceCommand";
import { sticker } from "./stickerCommand";
import { help } from "./helpCommand";

export const Commands = {
  all,
  dice,
  sticker,
  help,
};
